---
description: Cursor Agent adapter——同步执行或只读审查合格叶子并返回最小事实收据
alwaysApply: false
---

# Cursor Agent Adapter

本 pack 的 Cursor 方言在全局 `cursor_adapter_enabled` 允许时同步调用 Cursor CLI。writer / Reviewer 的能力下限、评分、额度池及 Review 责任由[统一模型路由](nr-agent-model-router-model-routing.mdc)决定；Cursor 宿主禁止递归调用 adapter，scored 候选用指定模型的 Task / 子 Agent。进程组、stream-json 解析与超时杀进程由同 pack 的 runtime 工具提供。Git 可见改动只作可选事实，不因此 fail-closed；验收由上层 Review 看 diff。

## 何时派发 writer

R1/R2 在需求、验收、产品/架构选择和根因完成后，只把自然形成、写域可冻结、有直接 oracle、无其他 writer 的叶子交给 Cursor。R0 不为 Cursor 拆票。已有 active runtime ticket 时沿用票面，没有时只临时冻结简报，不得为了 adapter 新建 runtime。

## 同步 writer 调用

Codex 宿主先运行以下入口；其原生子 Agent/角色列表没有 Cursor 不算不可用：

```sh
python3 .engineering-agent-rules/tools/host-agent-adapter/tools/cursor_agent_adapter.py preflight
```

`preflight` 检查全局策略、CLI、登录和 Auto 可见性；`globally_disabled` 仍可报告底层 CLI 已连接，但不可派发。主 Agent 用 router 的 `recommended` 填入模型和来源，再通过 stdin 发送冻结简报：

```text
run --repo ROOT --allowed-write-path PATH --required-tier weak|medium|strong
    --assessed-tier weak|medium|strong
    --required-reasoning-depth economy|standard|deep|maximum
    --assessed-reasoning-depth economy|standard|deep|maximum
    --receipt-key OPAQUE_ROUTER_KEY
    --quota-pool cursor_first_party|cursor_api
    --model MODEL --model-source auto|third_party|cursor_native
```

wrapper 启动前再次读取 `dispatch-policy.json`；禁用或策略损坏时不启动。允许时使用 `--print --force --trust --sandbox enabled --output-format stream-json`。`--allowed-write-path` 只写入简报，不当硬门禁。prompt 不进 argv、stdout 或 receipt。stdout 始终只输出一份最终 JSON receipt；长任务仅向 stderr 立即 flush 固定形态、脱敏的阶段/事件计数与 heartbeat，不透传 prompt、模型正文、工具参数、模型名、路径、diff 或原始 stream。run/review 的 `--timeout-seconds` 是无有效 JSONL 事件的连续静默上限，不是总墙钟上限；有效事件持续到达就续期，preflight 仍用绝对短超时。system init 形成 model receipt；成功结束但核对失败时返回 `model_receipt_mismatch`、`fallback_eligible=true`、`fallback_reason=model_identity` 和 `candidate_untrusted=true`，不冒充额度失败或可交付候选。身份匹配的成功终态才返回 `candidate_ready` 和 `upper_review_required: true`。

## 显式只读 Review

```text
review --repo ROOT --required-tier … --assessed-tier …
       --required-reasoning-depth … --assessed-reasoning-depth …
       --receipt-key OPAQUE_ROUTER_KEY --quota-pool …
       --model MODEL --model-source …
```

该子命令以 `--mode plan --sandbox enabled` 启动新的 Cursor 根 Agent，不带 `--force`，并要求终态为 `AGENT_MODEL_REVIEW: PASS|BLOCKED|UNAVAILABLE`。任何 BLOCKED 或 UNAVAILABLE 都不能 `review_passed`。

## 额度错误与回退

只有 `preflight` 非 `ready` 或 run/review receipt 明确 `fallback_eligible=true` 才可按同一 router 收据回退；非 fallback 错误停手上报。一般回退记录首选 `executor`、`receipt_key`、`status/reason`，保持 tier/depth，不手写模型或按宿主列表改选。`model_receipt_mismatch` 可能已经产生 delta：上层先核对冻结写域和 Git delta，再只消费 router 收据的 `adapter_retries.model_receipt_mismatch`；不得按模型名自行跳过 family，序列为空就停手。真实 Cursor quota/rate-limit 在返回收据前调用 `report-limit`。ChatGPT/账户不支持该模型只淘汰该候选。adapter 不保存 task-local mode、handoff 或历史。
