---
description: Codex Agent adapter——经 stdin `-` 同步调用 Codex CLI 并返回请求侧收据
alwaysApply: false
---

# Codex Agent Adapter

本 pack 的 Codex 方言在全局 `codex_adapter_enabled` 允许时同步调用 Codex CLI。writer / Reviewer 的能力下限、评分、额度池及 Review 责任由[统一模型路由](nr-agent-model-router-model-routing.mdc)决定；Codex 宿主禁止递归调用本 adapter。能力仍来自 `.codex/agents/*.toml`；`developer_instructions` 由 router 透出。不以 `codex debug models` 或 `models_cache.json` 当账户可用集。

## 何时派发

Cursor 宿主在 router 收据 `executor=codex_adapter`、写域/oracle 可冻结、无其他 writer、preflight 可用时调用。R0 不为 adapter 拆票。已有 active runtime ticket 时沿用票面，没有时只临时冻结简报，不新建 runtime。

## 同步调用

先运行 `preflight`，检查全局策略与 `codex` CLI；`globally_disabled` 仍可报告 CLI 已连接，但不可派发。主 Agent 用 router 的 `recommended` 填入模型和 `reasoning_effort`，再通过 stdin 发送冻结简报：

```text
run --repo ROOT --model MODEL --reasoning-effort EFFORT
    --required-tier weak|medium|strong --assessed-tier …
    --required-reasoning-depth … --assessed-reasoning-depth …
    --receipt-key OPAQUE_ROUTER_KEY --quota-pool codex_main|codex_spark
```

wrapper 启动前再读 `dispatch-policy.json`；禁用或策略损坏时不启动。允许时使用：

`codex exec - --json --ephemeral -s <sandbox> -m <model> -c model_reasoning_effort=<effort>`

stdin 必须是位置参数 `-`；成功 JSONL 无 model/effort，收据记录**请求侧**值。writer 用 `workspace-write`，显式 `review` 子命令仍走 `codex exec`（`read-only`），不用 `codex review`。`--allowed-write-path` 若保留只写入简报，不当硬门禁。Git 可见改动只作可选事实，不因此 fail-closed。验收由上层 Review 看 diff。

`You've hit your usage limit` / `turn.failed` → `report-limit` 并回退。ChatGPT 账户「not supported when using Codex with a ChatGPT account」只淘汰该候选。
