---
description: 共享 Agent 工作流——三路径、授权边界与审查门槛
alwaysApply: true
---

# 工作流

编辑前只选三条路径；`required_tier` 仅表示模型能力。规则、策略和安装器交付按强化路径审查。委派见[多 Agent 委派](nr-agent-delegation.mdc)。

| 路径 | 风险 | 典型范围 | 默认流程 |
| --- | --- | --- | --- |
| 快速 | R0 | 低风险、局部可逆 | 冻结角色 gate 命中则由统一路由选独立额度，否则 controller；最小 oracle。 |
| 标准 | R1 | 边界已知、有限步骤 | 声明能力/评分下限，统一路由按额度池选择；否则 controller。 |
| 强化 | R2 | 安全/隐私、并发/生命周期、持久化/迁移、跨进程/仓契约、权限、不可逆或核心假设不明 | 致命探针、独立审查；需持久 2–6 票或续接才用 runtime。 |

- 多文件或多步骤本身不升级；按真实风险、未知依赖和边界定级。内聚 strong 可是一票，不为 runtime 拆票。
- **冻结角色 gate：** 窄定位或行为、验收、oracle 已冻结且最多两个文件的微改，可由轻量独立池承接；统一路由结合评分门槛与额度选择，不凭模型名。图片先转文字约束。产品/架构、根因未明、公共契约、安全、并发/生命周期、持久化、跨模块设计或宽泛综合不得进入；不人为拆票。
- **路由收据：** 写生产文件前记录执行端；旁路看似合格的省额度执行端时写原因。不得先由 controller 完成大部实现再声称无合适叶子。
- 已授权且范围、验收、风险清楚就继续，升级不自动重复确认；仅在产品选择、不可逆迁移、外部副作用或权限/隐私决定未定时等待。
- 外部依赖、签名、权限、设备、真实 UI/运行环境或服务可能推翻方案时，先做最小真实探针，不先铺框架。
- **Review：** controller 写的 R0 自审；委派 writer 的 delta 只生成待审候选，上层在同一 fingerprint 由未参与写入且能力足够的 controller/Reviewer 核 diff、生产路径、验收与 oracle。R2、strong、规则/策略/安装器/安全/隐私和硬门禁豁免须独立只读审查；adapter 自评不能替代。
- **根因：** 稳定局部证据可直接修；证据矛盾、两次新探针仍不明，或涉及安全/数据/并发/持久化/跨进程/核心契约时才独立复核。推测不得写成根因。
- 只更新新结果、里程碑、阻塞或待决；短任务连续推进。长任务/多 Agent 先冻结目标、分母、验收、非目标、写域和停止条件；“继续”只恢复该封套。
- 纯只读、事实查询、无语义格式调整和确定性中间失败不自动增加 Agent。无新证据不重试；同一失败最多两次安全新探针，仍不收敛则复核、外查或上报。
