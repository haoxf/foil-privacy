---
description: 共享多 Agent 委派——简报预算、并行、审查/根因执行细则与降级
alwaysApply: false
---

# 多 Agent 委派

在启动实现、取证、根因、Review、探索或 verify-only 子 Agent 时使用本规则。是否必须委派，以及 Review/根因豁免，以[Agent 工作流](nr-agent-workflow.mdc)为准；有模板或本规则不等于必须委派。

- 主 Agent 负责范围、验证和整合；任务 Agent 只处理一个边界明确的单元，不扩大范围或改变 Git 上下文。
- 不委派重复工作。每个任务 Agent 单一职责；实现、取证和最终审查不得混成长期任务。Review Agent 默认不得继续委派；按[交付门禁](nr-delivery-gate.mdc)最低审查面只列 findings、证据、未确认项和结论，不复述任务。
- 简报采用指针优先的软预算：目标/非目标、验收/规格指针、冻结 commit/diff/文件范围、证据摘要和经授权原始产物的精确位置。给路径、行号和稳定引用；不粘贴完整会话、完整大 diff、大日志、整个结果包或未经限定的产物目录。同工作区 Agent 自读一手证据；无法访问才附必要摘录并说明。自包含是足以独立开工，不是贴齐材料。
- 根因 Agent 默认不得继续委派；只返回候选根因或“证据不足”、支持/反驳证据指针、反假设、一个下一步验证和未确认项，不复述任务或完整日志。
- 无可用子 Agent 时按清单显式自审，最终说明降级原因；不得假称独立审查。交付时须披露审查未完成。
- 委派和 Review 只在[Agent 工作流](nr-agent-workflow.mdc)所选路径有净收益或明确要求时启动；安装统一模型路由后，主 Agent 只声明任务适用面、能力/评分最低值和 writer/Reviewer 目的，由脚本结合共享额度池返回执行端，不按模型名或剩余额度手工硬编码。冻结窄探索/微改和其余自然形成的 R1/R2 叶子均可使用独立执行额度，不触发 runtime。任何委派 writer 的 delta 由上层接纳；strong 或强化风险票使用独立 Reviewer。一个稳定完整候选最多一次最终独立审查。finding 修复后只做聚焦 closure；局部变化只失效相关 oracle/review，只有共享契约、核心假设、依赖图或候选边界变化才整体失效。完整结果只核验，不重复审查未变化代码。若同一修订后仍暴露架构、状态所有权或生产接线错误，停止围绕原抽象循环，交回主 Agent 接管或 replan。
- 可调推理强度的 `max` 只用于边界明确、质量优先的最终安全裁决或冲突证据仲裁，不用于整项长任务；普通实现和审查按足够档位与范围选择。
- 仅两个以上单元独立且并行能实质缩短关键路径、降低等待或提供专业视角时才并行；启动、简报、整合和复核成本接近任务本身则顺序执行，不为小任务构造形式化 DAG。共享 API/编译单元/依赖/工程定义/manifest/构建缓存或测试设备时串行或隔离；Red lane 独占编译单元。不临时建设调度设施。
- 并行须声明验证所有权：共享编译环境或并入同一候选时子 Agent 不做 build/test，由主 Agent 在整合后统一验证；仅隔离且不并入同一候选时可定向验证，过重可派 verify-only。
- 不因单次短暂无输出判定子 Agent 超时；结合任务范围、已报告进度和可观察证据。基础设施错误时可终止，缩小为自包含边界后重派一次；再次失败才按无可用独立 Agent 降级。任何 provider/model fallback 都不得低于冻结票面的 `required_tier`；可能写入后的失败必须先审计，不能因重试或降级覆盖未知改动。具体 provider 的模型选择、额度与网络恢复顺序由对应可选 pack 约束，不进入共享 core。
- 同一文件/共享架构边界不并行写；同一共享环境不并行冲突测试。
