---
description: 共享卡住处理——假停、待决账本与有界重试；不定义调度或 Git 授权
alwaysApply: false
---

# 卡住处理

实施中的假停、待决与重试细则。能否继续见[Agent 工作流](nr-agent-workflow.mdc)，能否提交见[Git 工作流](nr-git-workflow.mdc)。本文件不定义调度模式，也不授权 Git 动作。

- 在已确认范围内，持续处理可复现的实施失败和可安全重试的环境故障。
- 开始时保存 Agent 工作流定义的目标封套。连续推进不是范围扩张授权；当前验收以外的优化、重构、工具或新阶段进入后续事项账本。只有它们阻塞当前里程碑且修复不改变封套时才能直接纳入，否则暂停取得新决定。
- 缺少临时测试输入时，若任务语义不依赖用户真实数据，可生成最小、无敏感信息且可撤销的测试夹具继续验证。优先通过任务接口或任务自有临时位置注入，不修改系统共享状态；若必须修改剪贴板等共享瞬态状态，应先记录原值，并且仅在完成时仍等于本任务写入值时恢复，检测到用户或其他进程写入的新值时不得覆盖。非任务自有应用状态仍需明确授权；需要真实用户内容、凭据或产品判断时仍须按待决账本处理，不得在该路径上假设继续。
- 对外部应用、网络请求和异步 UI 明确建模 `starting/loading/ready/failed`。以可观察证据和有界超时轮询，不把转圈、控件尚未挂载或一次状态读取为空立即解释为失败，也不得无限延长等待。
- 若模块持续 `busy` 且日志、状态或产物在一个有界观察窗口内没有推进，先保存尾部证据，再调用项目已定义的安全取消或清理入口；取消后检查健康状态，仅在仍不健康且重载不会影响用户状态时，才重载本任务拥有的本地进程并重试一次。不得用反复重启掩盖可复现的实现缺陷，也不得终止不属于本任务的用户进程。
- 构建、测试和诊断的存活判断应使用阶段性证据：日志阶段、已枚举/执行测试、结果包、目标产物或 sampled stack 是否推进。单纯 CPU 忙、进程存在或墙钟未超时不等于有效进展；超过项目历史基线或预设阶段预算且证据不推进时，应采证、取消并诊断，不能无限等待或原样重跑。选定测试实际执行为零不得算通过。
- 已知的重载断连或退出签名应按项目运行手册解释：先做健康检查，再判断成功或失败。具体错误字符串、热键和取消函数属于项目或 capability 规则，不写入通用 core。
- 子 Agent 因网络、额度、进程或工具通道异常中断时，默认从现有 diff、证据和最后可信检查点续做，不从头重做。先确认旧写入者已停止且不再可写，再以同一目标封套、已完成/未完成清单、已证失败和有效证据重派；状态不可证明时才回退到 durable checkpoint/patch 或 quarantine。不得以推进为由让 controller 接管该票大部实现。仅当重派会重复同一失败、且剩余为已冻结至多两文件的聚焦修补时，才改记 writer=controller；R2 仍须独立审查。
- 需用户决策时须按会话内待决账本处理：先记入当前会话内的待决账本（现象、已尝试、需要的决定、挡住的里程碑/票；不落盘为长期 `Docs/blocked.md` 之类文件，跨会话仅在需要时并入既有 handoff），保留工作状态；不得假设未决语义继续写与之耦合的代码。然后扫描当前冻结封套内是否仍有与该决定解耦、可安全推进的事项（下一里程碑，或已纳入当前冻结封套的下一票）。封套内仍有解耦可推事项时可续推；仅当封套内已无可解耦可推事项时才停止，并一次性列出全部待决。同一冻结封套内的下一票默认可续推；与会话交接护栏同时安装时以此为准，仍不得「顺便」扩 scope 或拉入封套外的票。不得为绕开停机而发明新工作，也不得把后续事项账本中的优化自动拉入。
- 硬停耦合路径的清单见 Agent 工作流。出现那些卡点时，即使封套内尚有其他事项，也不得在未决假设上继续写耦合实现；应硬停该耦合路径，或仅推进明确不依赖该决定的事项：安全/隐私选择、外部凭据、不可逆迁移、会翻转架构/契约/公共 API/持久化语义的产品方向。
- 长任务中的「可验证阶段」指冻结的工程里程碑或用户可运行切片，不是一次文件读取、工具调用、编辑动作或内部判断。每个阶段结束后汇报已完成事项、当前证据和下一步；开始预计超过约一分钟的操作前，也应先说明目的与预期产出。连续更新仍须遵守 Agent 工作流的沟通粒度，不得用微型播报代替推进。
- 长任务进度使用冻结的里程碑分母，并分别报告工程里程碑与用户可运行/production 路径；不得把新增阶段加入分母后继续沿用旧百分比。若连续多个内部里程碑仍没有可演示的垂直切片，应停止继续扩建基础设施，重新评估最短用户路径。
- 迭代期只运行能证明当前结论的直接 oracle；候选稳定后按 Agent 工作流所选路径由 controller 检查或做独立审查，再按交付门禁运行一次覆盖最终 diff 的适用映射。完整结果需要独立核验时只做 evidence-only attestation；不要在每个微小修正后重复整套 L1/L2 或未变化代码审查，项目明确要求的高风险例外除外。
- 同一阻塞经两次基于新证据的不同安全尝试仍未解决时，不得原样反复重试；应向用户报告失败现象、已尝试方式及结果、当前判断和需要的决定或安全备选路径，并遵守 Agent 工作流的**卡住刹车**（无新证据立即禁止同向重试；针对同一失败签名/假设，有新证据最多两次后根因/外查/上报）。凭据、签名、权限、外部服务或数据风险等确认需要人工授权时即报告，无需等待两次尝试。
- 向用户报告阻塞或请求决策前，先检查是否仍有不扩大授权的恢复路径：构造可撤销测试输入、取消陈旧流程、重载本任务拥有的本地进程、基于新证据重试；若需人工决策，按会话内待决账本先记账并尝试解耦续推。只有这些路径均不适用、已失败，或封套内已无可解耦可推事项时才停止。
- 进度、阻塞或待决记账不等于标记任务为 `blocked`。若仍存在明确、安全、不需要新授权且与待决解耦的路径，可说明后继续；任务状态仍按适用的 blocked 门槛处理。
