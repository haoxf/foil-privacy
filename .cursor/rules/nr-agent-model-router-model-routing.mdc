---
description: 模型路由协议——两层评分缓存、异步额度快照、显式能力门槛、cursor_session 派发与上层 Review 闭环
alwaysApply: false
---

# Agent Model Router

本 pack 把“需要什么能力”和“当前让谁执行”分开。主 Agent 持有范围、风险、产品/架构判断和验收，脚本只对冻结要求、机器评分与额度事实求交集，不从模型名字猜能力，也不改变任务风险。

router 的结构化候选是一次调度的唯一能力收据：adapter 与上层只原样消费候选中的模型、来源、tier、额度池和评分证据，不再各自维护模型名→能力、模型名→额度池、启用状态或 Task slug 白名单的影子表。`tier_policy.py` 是三档顺序/比较的唯一实现，adapter 与 task runtime 也直接调用它；schema enum 只是受测试约束的边界快照。缓存刷新、失效和候选求交全部封装在工具内；Agent 不直接读写缓存，也不手工重算结果。

## 调用契约

选择 writer 或 Reviewer 前，主 Agent 先给出：

- `task-class`：`exploration | micro_edit | bounded_implementation | prototype | complex_implementation | root_cause | high_risk_review`；它只描述工作类型并约束角色适用面，不代替能力、推理深度或评分。
- `root_cause` 只表示根因尚未建立的只读证据调查：writer 必须 fail-closed，reviewer 目的改为调查稳定的故障证据包，而不是审实现 diff；根因建立后按实际改动重分类为 bounded/complex implementation。Cursor adapter 的 review 仅审实现候选，因此 root-cause 只选择 Codex 的只读 reviewer 角色。
- `required-tier`：`weak | medium | strong` 能力下限。
- `reasoning-depth`：`economy | standard | deep | maximum` 推理投入下限，与能力 tier 正交。对 Codex 固定角色分别映射到 Medium/Low 一类轻量 effort、High、XHigh、Max；Cursor 显式 variant 只从运行时 model id/display 的一致 effort 证据映射，无显式或冲突 effort 的静态模型 fail-closed 排除，Auto 只记 `economy`。脚本优先选择满足要求的最低深度，不能因 provider 偏好、额度、文件数或普通 strong 标签越级。
- 零个或多个 `--min-score DIMENSION=MINIMUM`：模型必须逐项达到的机器证据。支持 `overall_score`、`task_score`、`cursorbench`、`deep_swe`、`frontier_code`、`terminal_bench`、`apex_agents`、`aa_intelligence`、`gdpval_aa`、`apex_swe`、`aa_briefcase`、`harvey_lab`。
- `--prefer-provider auto|codex|cursor`：当前任务显式 provider 软优先；用户在当前任务说“优先使用 Codex/Cursor”后，后续每次 `select` 都传对应值，直到用户更改或取消，并覆盖全局默认。省略参数时读取 `~/.config/engineering-agent-rules/dispatch-policy.json`；文件缺失默认 `auto` 且两个 adapter 启用，损坏则安全降级为 `auto` 且两个 adapter 禁用并在收据标记 `invalid`。缺键只在内存补全，不改写磁盘。偏好只在已通过任务适用面、writer/reviewer、required tier、reasoning depth、逐维评分与额度门禁，且处于最低充分推理深度、**当前宿主允许且对方运行时可用** 的候选之间改变排序，不能强行选用不合格、已耗尽、更深档或不可用的 provider；回退时收据必须包含 `preference.honored=false` 与机器生成原因。
- `--host cursor|codex|unknown`：当前 Agent 宿主。省略时从环境检测：`CURSOR_AGENT` 为 Cursor，`CODEX_SANDBOX` / `CODEX_CI` 为 Codex；冲突或全无则 `unknown`。显式值覆盖检测。
- `purpose=writer|reviewer`；Reviewer 另传 `--writer-model`，脚本会降低同模型优先级，但独立性的硬条件仍是不同 Agent 身份和稳定候选。

```sh
python3 .engineering-agent-rules/tools/agent-model-router/tools/agent_model_router.py \
  select --repo . --task-class complex_implementation --required-tier strong \
  --reasoning-depth deep --purpose writer --prefer-provider codex
```

本机菜单栏 App 和人工 CLI 只通过同 pack 的单一工具原子读写全局策略，不直接拼 JSON：

```sh
python3 .engineering-agent-rules/tools/agent-model-router/tools/dispatch_policy.py get
python3 .engineering-agent-rules/tools/agent-model-router/tools/dispatch_policy.py set \
  --prefer-provider auto
python3 .engineering-agent-rules/tools/agent-model-router/tools/dispatch_policy.py set \
  --cursor-adapter enabled --codex-adapter enabled
```

路由先识别宿主，再探测对方是否可调用，禁止再拉起**同一个**运行时：

- Cursor 宿主产出 `cursor_session`：指定 `model_id` 的 Task / 子 Agent，**禁止** Cursor adapter（自己调自己）。这不是父会话窗口；派发见 `cursor_session` 派发。Codex 仅当全局 `codex_adapter_enabled=true` 且 `codex` CLI 可用时以 `executor=codex_adapter` 进入候选；上层走已安装 Codex adapter，不得自己发明 `codex exec`。
- Codex 宿主产出 `codex_agent` 角色（本宿主）。Cursor adapter 仅当全局 `cursor_adapter_enabled=true` 且 `agent` CLI 可用时进入候选。
- `unknown` 宿主禁止两个 adapter 和 Cursor 会话候选，避免误递归；Codex 仍仅在 CLI 可用时以本机 `codex_agent` 出现。

`cursor_adapter_enabled=false` 只关掉 **Codex→Cursor adapter**，不删除 Cursor 宿主的会话模型，也不退出 Cursor App。`codex_adapter_enabled=false` 只关掉 **Cursor→Codex adapter**，Cursor 宿主不再推荐 `codex_adapter`。当前任务显式 `--prefer-provider` 只能覆盖 provider 偏好，不能绕过自调禁止、对方 CLI 缺失或对应 adapter 禁用。偏好值在重新启用 adapter 后恢复生效。偏好按钮不得改写两个委派开关。

主 Agent 不应为了选中某个现有模型回调阈值。缺少指定维度即证据不足，不能用总分、厂商声称或名称补齐。`required-tier + reasoning-depth + task-class` 是普通路由输入；`--min-score` 只在某个公开 benchmark 维度本身就是本票真实能力要求时才传，不能把一组通用分数模板机械附到所有 complex/root-cause/Review，也不能因某档公开维度较完整就把任务升级到该推理档。评分卡回答“现有公开证据是否满足显式最低值”，不回答“本票应该使用 High、XHigh 还是 Max”。

`reasoning-depth` 由任务真实推理负载确定，不与 task class 机械绑定：冻结且路径清晰的普通强实现/Review 用 `standard`；确有深层状态流、已建立复杂根因或多约束验证用 `deep`；只有能指出 XHigh 不足所对应的代表性质量风险，或安全/权限/隐私/迁移/并发等最高风险最终 Review，才用 `maximum`。若不能证明需要更深档，保持较低档；用户说“优先 Codex/Cursor”只改变同一最低充分深度内的 provider 排序，永不升级推理档。

Spark 没有与当前权威表同口径的公开分数，只能在 `exploration` / `micro_edit` 的冻结角色契约满足且没有额外数值门槛时入选；指定任一评分最低值后，未评分角色自动排除。这样把“没测过”当作未知，而不是零分或合格。

## 两层全局评分

评分缓存为本机所有项目和会话共用。两个逻辑层同存于原子提交的 `model-benchmark-bundle-v1.json`，避免来源与评分卡混代：

1. `source` 是来源快照，保存固定 HTTPS 权威页面解析出的完整当前表格、版本/来源和抓取时间，不保存 HTML、cookie 或账户信息。
2. `scorecard` 是路由评分卡，由纯脚本把第一层加工成 canonical model key、总体分、任务分、原始维度、成本和证据继承关系。Agent 与路由器只消费第二层。

当前来源是 Cursor 官方 CursorBench 与官方模型对比页。多维数据可被同 family 的更高 effort 版本保守继承，并在评分卡标记来源；不能从更高 effort 反向赋给更低版本。Thinking 与非 Thinking 保持不同 key。CursorBench 若没有 Thinking 行，路由在查找时借用同档非 Thinking 的 CursorBench 分：`high` 及以下加固定政策增量 `+0.5`，且不得超过下一档已公布分减 `0.1`；`extra high` / `max` 不加增量（这些档位本身不能关闭 Thinking）。官方一旦公布 Thinking 行，精确 key 优先生效并停止派生。刷新必须先完整抓取、解析、验证并生成两层；成功才以单一原子 replace 提交 bundle，任何网络、schema、大小或写入失败都不替换 last-known-good。

评分默认 24 小时有效。读取永不等待网络：新鲜时直接返回；过期时立即返回 stale 并在后台单次刷新；冷启动返回 `warming` 并异步预热。跨进程锁和 2 分钟启动节流防止多会话重复刷新。需要安装后预热或诊断时才显式等待：

```sh
python3 .engineering-agent-rules/tools/agent-model-router/tools/model_score_cache.py refresh --wait
```

## 额度池和排序

额度按 `cursor_first_party`、`cursor_api`、`codex_main`、`codex_spark` 及未来 app-server 报告的额外 Codex 池分别归一化。Auto、Composer 与 Cursor Grok 使用 first-party 池；其它显式模型使用 API 池。Cursor 与 Codex 成功缓存均为 30 分钟；过期和冷启动同样 stale-while-revalidate，失败不修改 last-known-good。额度 envelope 带失效 generation；失效先推进 generation 再删除旧文件，读取和写回都须匹配当前值，因此删除中断或在途刷新都不能复活旧额度。只有 `cache.fresh: true` 的 `exhausted` 或本次真实 quota/rate-limit 可硬停池；stale 百分比只参与排序，不能熔断。

执行端若报告真实 quota/rate-limit，只把 router 候选的 `pool_id` 交回 `agent_model_router.py report-limit --pool POOL_ID`；统一工具自行失效正确缓存，调用方不定位、删除或刷新文件。Cursor adapter 与 Codex adapter 都已内建这一步。

Cursor 候选在每次 `select` 都重新取 CLI 账户模型总表，再只读 Cursor 本机设置中的 Agent 模型定义，以“`defaultOn` 或用户显式开启，再减显式关闭”得到当前实际开启 family；variant 先在完整 known family 中确定最长归属，再要求该 family 已开启，不能从已关闭的长 family 退化匹配已开启短 family。两份模型清单都不缓存，开关变化在下一次路由立即生效。未开启的模型即使高分也不得出现。设置不可读、schema 未知或具体 variant 无法映射时 fail-closed，不猜测开启状态；本机读取不访问网络，也不进入额度 TTL。

Auto 是底层型号不可观测的自适应路由器，不是一个可评分的 medium 模型。Cursor Router 会读取 query、上下文、复杂度和领域，所以冻结简报须如实写风险/跨度以帮助分类；但提示词不是强制型号协议，当前 CLI 也不公开底层型号或 Intelligence/Balance/Cost 选择。router 不给它填 benchmark 或 task score，也不允许它满足任何 `--min-score`；只按保守策略给予 `policy_ceiling: medium`，用于无数值门槛的窄探索、微改或普通有界实现。strong、根因、高风险 Review 或任一显式评分门槛必须使用有评分的显式模型。将来运行时可显式选择且证明 Auto Intelligence 时再单独建策略证据，不靠提示词猜 strong。

脚本随后排除未满足 tier、reasoning depth、任一最低评分、当前未开启模型或新鲜耗尽池的候选；先最小化超过要求的 reasoning depth，再在同一最低充分深度内综合用户 provider 偏好、任务适用面、池使用比例、评分、平均任务成本与 fast 偏好排序。这样 provider 偏好优先于默认成本/额度权重，但永不以更深推理档“满足”偏好；没有同深度合格偏好候选时回退并给出机器原因。冻结窄探索/微改在无数值门槛时优先独立 Spark 池；`codex_main` 在主池紧张时自然后移，但 required tier 和 reasoning depth 永不降级。固定 Codex 角色的 tier 来自受限角色契约；同型号/推理档位若能唯一对应第二层评分卡，可额外满足调用方显式指定的逐维最低值，但公开总分不反向改写该角色 tier。型号或档位无法唯一映射时仍按未评分角色处理。返回 `recommended`、最多四个 `alternatives`、`preference` 收据、深度证据、启用模型证据、评分/额度新鲜度和最小 pool facts；上层不读写缓存文件。

## cursor_session 派发

Cursor 注入给 Task 的 `model` 白名单短于 CLI / 本路由的 `model_id`。这是宿主限制，不是模型未开启。路由与上层都不维护 Task slug 影子表。

路由选档不变：不得因偏好、额度或省事把 High 升到 Extra High / Max。仅当平台明确拒绝该 slug（`Invalid model selection`，或等价「无法解析为子 Agent 模型」）时，按下面顺序再开 Task。这仍是不同 Agent。档位只对照收据字段与当前会话模型说明，不另造映射表。

1. 先把收据 `model_id` 原样传给 Task。
2. 按同一 `reasoning_depth` 的 `alternatives` 原样再试。父窗口与收据同一 family 且同一档时，也可省略 `model`（inherit）。
3. 同深度都派不出去之后，可以改用更好的，不能改用更差的：同一收据里 `reasoning_depth` 严格大于 `recommended` 的 `alternatives` 原样再试（不重新 `select`）；父窗口同一 family 且不浅于收据时也可 inherit（Extra High 可以顶 High；High 不能顶 Extra High）。
4. 更好的路径也全部被拒 → 停手，列出被拒 slug。禁止把 writer 或 Reviewer 留在父会话窗口，也不得改用更浅档。

## 上层 Review

所有 writer 的非空 delta 都只是待审候选。上层在同一稳定 fingerprint 上再次调用 router（`purpose=reviewer`，门槛不得低于 writer/风险），同一 Agent 身份被硬排除而非只降权，然后选择：

- `reasoning_depth=standard` 的 `strong_reviewer`（High）；
- `reasoning_depth=deep` 的 `critical_reviewer`（XHigh）；
- `reasoning_depth=maximum` 的 `max_reviewer`（Max）；
- router 返回的合格 Cursor 模型：Cursor 宿主按上一节派发指定 `model_id` 的 Task / 子 Agent（`executor=cursor_session`）；Codex 宿主仅在 adapter 可用时走 adapter（`executor=cursor_agent`）的显式只读 `review` 子命令。
- router 返回的合格 Codex 角色：Codex 宿主用本宿主角色（`executor=codex_agent`）；Cursor 宿主仅在 Codex adapter 可用时走 adapter（`executor=codex_adapter`）。Codex 收据键规范化为 `(model_id, reasoning_effort)`。

`cursor_session` 不是当前父会话窗口。父会话没有可验证模型收据，不能冒充 strong。上层按上一节开不同 Agent（Task）；Reviewer 尤其不得留在 writer 的同一聊天窗口。`receipt_key` 只用于对齐目标模型，不证明当前窗口已经是该模型。weak/medium 的 controller 直接检查仍是工作流内检查，不冒充独立候选。

Cursor adapter 不再在一次 writer 调用里强制嵌套原生 Reviewer；`candidate_ready` 只证明模型、同步终态合格，并带 `upper_review_required: true`。显式 Cursor Review 使用 plan/read-only 模式和 `AGENT_MODEL_REVIEW` 终态；tier 不足、BLOCKED/UNAVAILABLE 都不能闭环。Git 可见改动只作可选事实。

普通候选一次 Review、一轮聚焦修复后一次 closure；高风险票按共享交付门禁增加独立视角。不得因额度紧张省略审查、让 writer 自审或把测试冒充 Review；也不得在 adapter 内部与上层重复机械 Review。
