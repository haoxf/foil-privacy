---
description: 共享编码纪律——先理解和前置真实探针，后做最小可验证改动
alwaysApply: true
---

# 编码纪律

宁可做小而理解充分的改动，也不要为假设中的灵活性过度设计。

根目录有 `CONTEXT.md` 时，将其作为领域术语真源而非规格。

1. 编辑前阅读相关调用链。说明重要假设；若选择会改变范围、可见行为、数据语义或安全性，先询问。
2. 从第一个足够的方案开始：不做；复用仓库代码；使用平台/库能力；做最小局部改动；最后才新增代码。
3. 外部平台、依赖、权限、设备、服务、真实环境或 UI 先查一手资料并做能推翻方案的最便宜探针。环境相关功能先打通最薄真实切片；无法实跑须留未验证项，不用单测冒充。自动测试禁访真实服务不免实测：依赖获授权只读低成本真实调用的新工具，提交前须端到端 smoke；`unknown` 只证安全降级，不证功能可用。
4. 不为单一用途引入抽象。只有出现三处真正相似的用法，或现有共享抽象明显就是扩展点时，才收拢。
5. 只改请求所需内容。保留无关的用户修改和既有风格。修根因，不只遮住某个调用方。
6. 先定义可观察成功标准。oracle 可为现有测试、编译/静态检查、一次性反例、CLI/App/Simulator/真实系统实跑、截图、日志或人工 smoke；不等于新增永久测试。永久测试只覆盖用户可观察长期契约、已发生回归、高风险边界或难重复的关键路径；Reviewer 理论反例默认一次性验证。断言可观察行为，不测实现细节；删掉重复、低信号测试。候选稳定前只跑能证明结论的最小验证；交付时再按门禁跑一次适用映射。
7. 多职责/难审文件不得增加功能逻辑或新职责；最小局部修复可留原位，能拆则拆，扩围须获用户接受。不要为凑行数拆分；同一职责分声明/分文件做数字搬家是假拆，按完整职责单元审查。
8. 若有刻意的、具有实质限制的捷径，在代码附近记录限制及升级路径。
9. 防护与交付工具也受 YAGNI 约束。lint、边界检查或测试支架若开始模拟解析器、持续误报或主导交付成本，停止补丁式扩张，改用编译器、模块/target 边界或成熟工具。
10. 一个事实、一项业务职责或一条状态/数据/生命周期链只设一个权威拥有者和主架构。可由脚本唯一计算或机械执行的解析、合并、刷新、迁移和派生值由单一工具持有；Agent 只提交语义要求并消费结构化结果。规则、adapter、UI 和调用方只引用，不复制映射、阈值、状态机或判断算法，也不让新旧架构、双入口、双写/互同步长期并存。迁移须明确替代关系、唯一切换点、数据兼容边界和旧路径删除条件；冲突时先确定拥有者、删除影子真源并收敛调用链，再修症状。只有风险、产品、架构与语义审查保留给模型。
