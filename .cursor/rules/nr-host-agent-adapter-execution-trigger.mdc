---
description: Host adapter 路由——跨宿主派发冻结叶子；禁止自调
alwaysApply: true
---

# Host adapter

禁止自己调自己。经[统一路由](nr-agent-model-router-model-routing.mdc)选中后：收据 `executor` 为 `cursor_agent` 时加载[Cursor 协议](nr-host-agent-adapter-cursor-agent-adapter.mdc)；为 `codex_adapter` 时加载[Codex 协议](nr-host-agent-adapter-codex-agent-adapter.mdc)。另须策略允许、写域/oracle 可冻结、无 writer、preflight 可用。`cursor_session` 用指定模型开 Task。R0 不为 adapter 拆票。

`candidate_ready` 只是待审候选，上层须完成独立 Review；判断未冻结或工具不可用时旁路。
