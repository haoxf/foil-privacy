---
description: Host adapter 路由——跨宿主派发冻结叶子；禁止自调
alwaysApply: true
---

# Host adapter

禁止自己调自己。路由 `status=ok` 时先试 `recommended`：Codex 宿主的 `cursor_agent` 加载[Cursor 协议](nr-host-agent-adapter-cursor-agent-adapter.mdc)，Cursor 宿主的 `codex_adapter` 加载[Codex 协议](nr-host-agent-adapter-codex-agent-adapter.mdc)；原生角色列表看不到对方不算不可用。`cursor_session` 开 Task，`codex_agent` 用本宿主角色。`alternatives` 仅在协议认可的结构化失败后回退，保持 tier/depth 并记录首选收据；非可回退错误停手上报。

`candidate_ready` 只是待审候选，上层须完成独立 Review；旁路仅 R0/冻结角色 gate 或上述失败。
