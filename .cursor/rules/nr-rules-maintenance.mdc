---
description: 共享规则维护——规则变更后同步项目路由、引用与托管状态
alwaysApply: false
---

# 规则维护

新增、删除、重命名或实质改变项目规则职责时，维护项目的轻量入口与引用；规则正文仍应保留在各自 `.mdc` 文件中。

1. 更新项目 `AGENTS.md` 或等价路由中的场景、规则路径和高频摘要；不要把完整规则正文复制进去。
2. 删除或重命名规则后，定向搜索旧路径，更新 router、交叉引用、命令说明与稳定入口；确认没有失效链接。
3. 托管规则由安装器管理时，使用该项目规定的更新与校验命令；不要手改安装器拥有的规则或 lock 记录。
4. 仅修改正文且路径、职责和入口均不变时，不必机械修改 router；但高频摘要不再准确时必须同步。
5. 交付或提交前，确认新增规则可被唯一定位、退役规则已移除引用，且项目私有 overlay 仍只包含项目细节。
6. 修改项目私有规则后，通过项目记录的安装器入口运行消费者检查，例如 `python3 <规则源仓库>/scripts/rulesctl lint-consumer --target <项目>`；修改共享源后先在源仓库运行完整门禁，再用同一入口依次执行 `update --dry-run`、审查差异、正式 `update` 和 `verify`。不要假设消费者仓库自带 `scripts/rulesctl`，也不要手工复制托管文件冒充同步完成。

项目私有规则负责具体 router 结构、稳定路径与本项目安装命令。
