---
description: 统一模型路由——按能力、深度、评分与额度选择执行端
alwaysApply: true
---

# 统一模型路由

选择 writer/Reviewer 前冻结 `task_class`、`required_tier`、`reasoning_depth` 与评分下限，再按[模型路由协议](nr-agent-model-router-model-routing.mdc)调用脚本。省略 `--prefer-provider` 时读全局偏好；用户指定后每次传入。Cursor 宿主须传 Task 短名单，缺则不派 `cursor_session`。返回最低充分深度候选；不能自调，偏好/额度不能把 High 升到 XHigh/Max。

过期缓存立即用于本次判断并异步刷新；只有新鲜缓存或真实运行错误可硬停池。写入候选须上层统一 Review 后才能交付，adapter 的 `candidate_ready` 只表示可审候选。
