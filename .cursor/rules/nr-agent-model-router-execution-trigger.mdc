---
description: 统一模型路由——按能力、深度、评分与额度选择执行端
alwaysApply: true
---

# 统一模型路由

选择前按[模型路由协议](nr-agent-model-router-model-routing.mdc)冻结门槛并调用脚本。`status=ok` 时须先试 `recommended`；`alternatives` 仅供结构化失败后按 router 的 retry 收据回退。跨宿主不以原生角色列表判断可用性：`cursor_agent` / `codex_adapter` 分别走 Cursor / Codex adapter；`cursor_session` 开 Task，`codex_agent` 用本宿主角色。Cursor 宿主须传 Task 短名单。

过期缓存立即用于本次判断并异步刷新；只有新鲜缓存或真实运行错误可硬停池。写入候选须上层统一 Review 后才能交付，adapter 的 `candidate_ready` 只表示可审候选。
