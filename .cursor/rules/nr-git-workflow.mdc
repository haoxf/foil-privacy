---
description: 共享 Git 安全规则——保持用户上下文，过闸任务改动默认本地提交
alwaysApply: true
---

# Git 工作流

任务开始记录仓库、分支/detached、HEAD 与 staged/unstaged/untracked。

- 保持用户分支/worktree，不把用户当前 worktree 自动收编。只有单独授权或 active runtime 的精确 `开始执行` 与冻结 `git_policy` 才可创建/切换分支；仅限本地 branch/逐票 commit/ff-only。worktree 仍需 task 前事先单独授权，`开始执行` 不能代替。
- 已获实施授权时，过闸且可独立回滚的任务改动默认本地 commit，不按路径或是否 runtime 区分。精确暂存本单元 hunk，提交前跑交付门禁；失败或未验证不提交。人工验收单元在「验收通过」前不 commit。写明不要自动提交或 `commit_policy: none` 则让路。
- 不 stash/restore/reset/clean/rebase/fetch/pull/普通 merge/删分支/改写历史/强制移动 ref；policy 不授权 push、PR、release 或外部动作。
- 既有修改默认属用户；任务 patch 只含当前请求产生或明确纳入的 hunk。
- 分支被并发推进时，若原 HEAD 仍是祖先、新 commit 不与本任务假设或 hunk 重叠，则自动重基线到新 HEAD、在新 HEAD 上完成最终审查与验证后继续，不要求用户再次确认；只有祖先关系破坏、真实重叠或任务假设失效时才停止并报告冲突。
- runtime 每次写入和每个 checkpoint commit 都按真实 Git 与 runtime history 重验冻结 branch 生命周期矩阵；歧义停为 needs_replan。
- 任务 commit 禁止使用 `git add .`、`git add -A` 或 `git add -u` 等整体暂存。
- 若既有暂存内容或混合 hunk 无法可靠分离，应停止，不要用 Git 清理命令绕过边界。
