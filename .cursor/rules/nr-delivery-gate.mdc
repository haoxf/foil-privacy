---
description: 共享交付门禁——按三路径审计稳定候选、证据、审查和 Git 契约
alwaysApply: false
---

# 交付门禁

在明确授权的 commit 或 merge 前，或冻结 `git_policy` 已允许的本地 commit / ff-only back 前运行本门禁。逐票 checkpoint 只使用当前 ticket 的 scoped Git audit、直接 oracle 与适用审查，不重复 run 完整映射；最终集成仍须保持 `RUN_GATE_COMPLETE`、候选、Git 上下文与 ancestry 未漂移，否则只报告 `integration pending`。

稳定候选按一次收口：完成实现与直接 oracle → 冻结 diff → 按[Agent 工作流](nr-agent-workflow.mdc)检查/审查 → 只修阻塞 finding 并聚焦复核 → 对最终候选运行一次适用映射。用户可见或环境相关功能的真实 smoke 应在实现早期完成，最终门禁只复跑或汇总确认，不得首次发现核心链路不可用。

1. **候选与 oracle：** 核对验收、生产调用链和权威契约。oracle 可用现有测试、编译/静态检查、一次性脚本、CLI/App/Simulator/真实系统实跑、截图、日志或人工 smoke；新增永久测试须满足[编码纪律](nr-coding-discipline.mdc)§6，不因代码或 Reviewer finding 自动新增。
2. **定向验证：** 只运行能证明当前结论的检查。若选择测试，须确认 selector 确实枚举并执行预期用例；零测试或包装脚本成功不算通过。impact/manifest/路径映射只是候选集，须结合真实 diff、行为所有权和可达性判断；不得用明显无关的全量回归掩盖错误映射。
3. **范围与 Git：** 将 diff 与任务基线对照，确认每个 hunk 属于本任务且不依赖无关脏改动；重验获授权 branch/worktree/HEAD。不要用 stash、reset、restore 或整体暂存掩盖不确定性。
4. **审查频率：** controller 亲自写的快速路径自审；任何委派 writer 的候选都由上层选择未参与写入且能力足够的 controller 或 Reviewer，重读实际 diff、生产路径和冻结验收并持有 oracle后才能接纳。标准路径只审一次稳定完整候选；执行 adapter 返回的 ready/self-review 不能跳过上层接纳。强化路径、strong 票及规则/`rulesctl`/策略/安装器/安全/隐私、机器硬门禁豁免记录须独立只读 Review。Reviewer 读一手 diff、职责上下文、契约和失败证据，不只读摘要。
5. **阻塞标准：** finding 只有直接违反验收、在支持环境真实可复现、存在明确安全/数据/状态/并发风险、阻塞下一个纵向切片，或与当前生产路径直接相关时才阻塞。理论攻击、非支持环境、纯整洁偏好和无产品影响的极端边界记录为 deferred，不无限硬化当前任务。结构问题仅在新增不内聚职责、继续扩大难审多职责文件、假拆，或项目机器结构硬门禁失败时阻塞；最小局部修复不因此扩围。结构结论写 `结构检查：通过|阻塞|用户已明确接受`；适用机器门禁另写 `结构门禁：通过|阻塞|用户明确豁免`。豁免记录仍须含原因、批准人、可定位 `approval_ref` 并独立审查，Agent 不得自行批准。
6. **修正与失效：** 局部实现或测试修正只失效 finding 涉及的 oracle/review，复审只看该范围；共享契约、核心假设、依赖图或候选边界变化才失效下游证据并允许整体重审。交付后发现的独立新问题进入新票，不重开无关已完成票。禁止仅为“证据一致性”无条件重跑所有票、所有测试或所有审查。
7. **最终验证：** 对审查后稳定候选运行一次项目要求的相关 lint、构建、测试等级和机器硬门禁。若结果促使候选变化，先运行受影响 oracle；候选再次稳定后只补项目仍要求的最终映射，不重审未受影响代码。结果包或日志摘要变化不触发代码复审。
8. **证据核验：** controller 核对结果与计数；项目明确要求时才做 evidence-only attestation。无可用独立 Reviewer 时不得假称完成强化审查，须披露降级。

提交前汇报候选范围、排除的既有改动、Git 上下文、审查结论与契约状态，并分开写：

```text
Verified: <已跑 oracle、范围、结果>
Not verified: <应验证但未跑的项>（reason: …）
```

不得把未跑写成通过；纯文档/规则且无行为变化时，可写明跳过构建/产品测试的依据。
