---
description: 源码文件体量硬阈值门禁——已安装则映射验证必须跑检查器；新建、首越或存量增长默认失败
alwaysApply: false
---

# 源码文件体量门禁

本 pack 面向 Python/C/C++ 等非 Swift 源码。项目显式安装后即成为映射验证的必跑项：使用已安装的默认 profile，不必再复制 `project.json`。工具缺失、未运行、无显式 `--baseline-ref`、基线无法解析、度量或 allowlist 解析失败，均不得报告验证通过。

## 硬阈值

- 只检查相对 `--baseline-ref` 的变更：`git diff` 中的已跟踪路径，加上 `git ls-files --others --exclude-standard` 的未跟踪路径。不 walk 全仓库，也不自解析 `.gitignore`；忽略规则以 git 为准。无匹配变更视为通过。
- 度量对象是仓库相对 `file_key` 的物理行数：空文件为 0；否则换行字节数加上没有结尾换行时的 1。含 NUL 的文件视为二进制，直接排除，不度量、不因此失败。不剥注释、不解析函数/AST，也不要求 clang。
- 命中：新建候选 `>= threshold`，基线 `< threshold` 且候选 `>= threshold`，或基线已 `>= threshold` 而候选继续增长。`threshold` 默认值和最大值为 1000；项目可在 profile 选择 `1...1000` 的更严格单阈值，不得放宽。未改动的超阈存量不在本次变更集中，不检查。删除通过，重命名按旧键删除和新键创建处理。
- 默认 profile 以仓库根为 `source_roots`，并按 include globs 匹配；检查器内置排除 `.git/` 与 `.engineering-agent-rules/`。产物目录、依赖和中间文件归仓库 `.gitignore`。`exclude_globs` 只用于已跟踪但不属于本门禁的路径（例如 generated）。项目要收紧扫描范围时，再复制为私有 `profiles/project.json`。
- include/exclude 都以仓库相对 POSIX 路径匹配；`**/` 前缀在仓库根也匹配零个路径段。变更集里匹配 include 的符号链接失败；baseline 只为本次变更路径读取普通 blob。
- 禁止用切片文件做数字搬家规避阈值；职责拆分遵循 `coding-discipline` §7。

## 运行与证据

项目同时使用 Agent Task Runtime 时，controller 在 arm 前检查票写域与 profile `source_roots`；可能相交的票须把显式检查器 argv 写入 verification，并以冻结任务基线作为 `--baseline-ref`。候选冻结并送 Review 前运行预检，关闭票和最终关闭任务时按同一 fingerprint 复核。

```sh
python3 .engineering-agent-rules/tools/source-file-size-gate/tools/check_source_file_size.py \
  --root . \
  --profile .engineering-agent-rules/tools/source-file-size-gate/profiles/example.json \
  --baseline-ref <显式 git commit-ish>
```

`--baseline-ref` 必填且只接受可解析为 commit 的 commit-ish；不得省略或猜测 `HEAD~1`。profile 与 allowlist 各从一次普通文件 byte snapshot 解析并计算 hash，输出前复核未变。allowlist 文件缺失视为空豁免表，存在则必须是合法记录。候选原始 bytes 捕获到保留相对路径的隔离临时树，digest 与度量来自同一批 bytes；完成后重枚举并复核 live 路径集合、类型和内容。

stdout 为 schema 1 JSON：包含 gate/profile/allowlist hash 与阈值、baseline requested ref/解析 commit/digest、candidate root/digest/file count，以及 `pass|block|waived`、`hits`、`violations`、`waiver_evidence`。命中以 `file_key` 标识，kind 为 `new_file`、`first_cross` 或 `over_threshold_growth`。默认省略全量 sizes；仅局部诊断时可加 `--include-all-sizes`。

## 豁免

默认不要求仓库里已有 allowlist。需要放行时再写 `Docs/source-file-size-allowlist.json`，形如 `{"schema_version":1,"waivers":[]}`。每条记录必须且只能含 `file_key`、与 profile 一致的 `threshold`、不低于阈值的整数 `max_size`、`reason`、`approved_by`、`approval_ref`；重复键、未知字段和空字段失败。只有候选 `<= max_size` 才豁免，超过上限仍阻塞。

会话同意不能口头放行。新增或扩大豁免须有针对本次 `file_key + max_size` 的可定位批准依据，并由独立 Reviewer 核验；Agent 不得代填批准人自行批准。

## 交付结论

检查通过写 `结构门禁：通过`；违规写 `结构门禁：阻塞`。allowlist 精确命中时检查器写 `结构门禁：豁免记录命中`；只有独立核验批准证据后，最终交付才可写 `结构门禁：用户明确豁免`。
